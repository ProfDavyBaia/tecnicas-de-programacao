{"title":"Notas — Aula 01: Estruturas de Dados — Pilhas e Filas","markdown":{"yaml":{"title":"Notas — Aula 01: Estruturas de Dados — Pilhas e Filas"},"headingText":"Introdução","containsRefs":false,"markdown":"\n\n\nEsta aula explora dois dos tipos de estruturas de dados lineares mais\nimportantes da computação: **pilhas** e **filas**. O percurso segue uma\nprogressão lógica do conceito à prática: fundamentos, pilhas, filas,\naplicações práticas em engenharia e exercícios de fixação.\n\n## O que são estruturas de dados\n\nUma **estrutura de dados** é uma forma organizada de armazenar, gerenciar\ne acessar informações na memória do computador. A escolha correta da\nestrutura impacta diretamente o desempenho e a eficiência dos algoritmos.\nElas se dividem em três grandes famílias:\n\n- **Lineares** — elementos organizados em sequência: arrays, listas,\n  pilhas e filas.\n- **Hierárquicas** — elementos em relações pai-filho: árvores e heaps.\n- **Em rede** — conexões arbitrárias entre elementos: grafos e tabelas\n  hash.\n\n## Por que estudar pilhas e filas\n\nPilhas e filas estão presentes em sistemas operacionais, compiladores,\nprotocolos de rede, microcontroladores e sistemas de tempo real — todos\nfundamentais para o engenheiro elétrico moderno. Algumas aplicações\ndiretas: gerenciamento de interrupções em microcontroladores (Arduino,\nSTM32), buffers de dados em comunicação serial (UART, SPI, I2C), análise\nde expressões matemáticas em calculadoras, escalonamento de tarefas em\nsistemas embarcados e controle de fluxo em redes de computadores.\n\n## Pilhas (Stacks)\n\n### Conceito e princípio LIFO\n\nUma **pilha (stack)** é uma estrutura de dados linear que segue o\nprincípio **LIFO — Last In, First Out**: o elemento inserido por último é\nsempre o primeiro a ser removido, como uma pilha de pratos — sempre se\nretira e coloca pratos pelo topo.\n\nAs operações fundamentais, todas O(1):\n\n- **PUSH** — insere um elemento no topo da pilha.\n- **POP** — remove o elemento no topo da pilha.\n- **PEEK / TOP** — consulta o topo sem remover o elemento.\n- **isEmpty** — verifica se a pilha está vazia antes de operar, evitando\n  erros de underflow.\n\nO **ponteiro de topo** é o único ponto de acesso da pilha. Tentar remover\nde uma pilha vazia gera um erro chamado **stack underflow**; inserir além\nda capacidade gera **stack overflow**. Uma sequência típica de operações\n— `Push(A)`, `Push(B)`, `Push(C)`, `Pop()` — mostra o ponteiro de topo se\nmovendo a cada chamada.\n\n### Implementação em Python\n\nPython oferece duas formas principais de implementar pilhas: usando\n`list` nativa, ou encapsulando o comportamento em uma classe.\n\nCom lista:\n\n```python\npilha = []\n\n# PUSH - empilhar\npilha.append(10)\npilha.append(20)\npilha.append(30)\nprint(pilha)  # [10, 20, 30]\n\n# PEEK - consultar topo\ntopo = pilha[-1]\nprint(topo)   # 30\n\n# POP - desempilhar\nelemento = pilha.pop()\nprint(elemento)  # 30\nprint(pilha)     # [10, 20]\n\nif not pilha:\n    print(\"Pilha vazia!\")\n```\n\nCom classe orientada a objetos:\n\n```python\nclass Pilha:\n    def __init__(self):\n        self._dados = []\n\n    def push(self, item):\n        self._dados.append(item)\n\n    def pop(self):\n        if self.is_empty():\n            raise IndexError(\"Underflow!\")\n        return self._dados.pop()\n\n    def peek(self):\n        return self._dados[-1]\n\n    def is_empty(self):\n        return len(self._dados) == 0\n\n    def size(self):\n        return len(self._dados)\n\np = Pilha()\np.push(5)\np.push(10)\nprint(p.peek())  # 10\nprint(p.pop())   # 10\n```\n\n### Aplicação prática: verificador de parênteses\n\nUm dos usos clássicos de pilhas é a **verificação de expressões\nbalanceadas**, fundamental em compiladores e parsers de linguagens. O\nalgoritmo percorre a string, empilhando aberturas e verificando se cada\nfechamento corresponde à última abertura pendente:\n\n```python\ndef verificar_parenteses(expressao):\n    \"\"\"Verifica se parênteses, colchetes e chaves estão balanceados.\"\"\"\n    pilha = []\n    pares = {')': '(', ']': '[', '}': '{'}\n    abertos = set('([{')\n\n    for char in expressao:\n        if char in abertos:\n            pilha.append(char)\n        elif char in pares:\n            if not pilha or pilha[-1] != pares[char]:\n                return False\n            pilha.pop()\n\n    return len(pilha) == 0\n\nprint(verificar_parenteses(\"(a + b) * [c - d]\"))  # True\nprint(verificar_parenteses(\"(x + [y))\"))          # False\nprint(verificar_parenteses(\"{V = I * R}\"))        # True\n```\n\nEsse algoritmo roda em O(n), onde n é o tamanho da expressão, e é usado\nem IDEs, compiladores (Python, Java, C++) e em avaliadores de circuitos\nlógicos.\n\n### Pilhas em sistemas embarcados: call stack\n\nEm microcontroladores como **Arduino (ATmega328)** e **STM32**, a pilha\nde chamadas (*call stack*) gerencia automaticamente o retorno de\nsub-rotinas e o contexto de interrupções. Quando ocorre uma interrupção,\no estado dos registradores é empilhado; ao finalizar, é desempilhado.\n\n- **Stack Pointer (SP)** — registrador dedicado que aponta sempre para o\n  topo da pilha de hardware. Em ARM Cortex-M, cresce para endereços\n  menores (pilha descendente).\n- **Context Saving** — ao entrar em uma ISR (Interrupt Service Routine),\n  PC, LR e registradores são automaticamente empilhados pelo hardware\n  para preservar o estado do programa.\n\n## Filas (Queues)\n\n### Conceito e princípio FIFO\n\nUma **fila (queue)** é uma estrutura de dados linear que segue o\nprincípio **FIFO — First In, First Out**: o elemento inserido primeiro é\nsempre o primeiro a ser removido, como uma fila de atendimento bancário.\nA inserção ocorre no **fim (rear)** e a remoção na **frente (front)**.\n\nOperações fundamentais, todas O(1):\n\n- **ENQUEUE** — insere no final da fila (rear/back).\n- **DEQUEUE** — remove da frente da fila (front/head).\n- **FRONT** — consulta o primeiro elemento sem remover.\n\nA diferença fundamental em relação à pilha é que a **fila tem duas\nextremidades ativas**. Isso exige atenção especial na implementação com\narrays, onde o deslocamento de memória pode tornar o dequeue ineficiente\n(O(n)) se não for tratado com ponteiros circulares. Uma sequência típica\n— `Enqueue(A)`, `Enqueue(B,C)`, `Dequeue() → A`, `Dequeue() → B` — ilustra\nos elementos entrando pelo rear e saindo pelo front.\n\n### Implementação em Python\n\nPython oferece `collections.deque` como implementação otimizada para\nfilas, com operações O(1) em ambas as extremidades:\n\n```python\nfrom collections import deque\n\nfila = deque()\n\nfila.append(\"Tarefa_1\")\nfila.append(\"Tarefa_2\")\nfila.append(\"Tarefa_3\")\nprint(fila)\n# deque(['Tarefa_1','Tarefa_2','Tarefa_3'])\n\nprint(fila[0])  # Tarefa_1 (FRONT)\n\natendido = fila.popleft()\nprint(atendido)  # Tarefa_1\nprint(fila)\n# deque(['Tarefa_2', 'Tarefa_3'])\n\nprint(len(fila) == 0)  # False\n```\n\nTambém é possível implementar uma fila com nó encadeado, sem depender de\n`deque`:\n\n```python\nclass No:\n    def __init__(self, dado):\n        self.dado = dado\n        self.proximo = None\n\nclass Fila:\n    def __init__(self):\n        self.front = None\n        self.rear = None\n        self._tamanho = 0\n\n    def enqueue(self, item):\n        novo = No(item)\n        if self.rear:\n            self.rear.proximo = novo\n        self.rear = novo\n        if not self.front:\n            self.front = novo\n        self._tamanho += 1\n\n    def dequeue(self):\n        if not self.front:\n            raise IndexError(\"Fila vazia!\")\n        dado = self.front.dado\n        self.front = self.front.proximo\n        if not self.front:\n            self.rear = None\n        self._tamanho -= 1\n        return dado\n```\n\n### Variações de filas\n\nExistem variações especializadas da fila básica que atendem requisitos\nespecíficos de sistemas reais, especialmente em tempo real e em\ncomunicações:\n\n- **Fila circular** — utiliza um array com ponteiros `front` e `rear`\n  que \"giram\" em módulo, eliminando desperdício de memória. Ideal para\n  buffers de hardware de tamanho fixo.\n- **Fila de prioridade** — cada elemento possui uma prioridade associada,\n  e o dequeue sempre retira o elemento de maior prioridade,\n  independentemente da ordem de inserção. Normalmente implementada com\n  heap.\n- **Deque (double-ended)** — permite inserção e remoção em ambas as\n  extremidades, generalizando pilhas e filas. Em Python,\n  `collections.deque` implementa exatamente esse tipo.\n\n### Buffer circular: aplicação em UART\n\nEm sistemas embarcados, o **buffer circular (ring buffer)** é a\nimplementação de fila mais utilizada para comunicação serial UART. A ISR\nde recepção faz *enqueue* dos bytes recebidos; a rotina principal faz\n*dequeue* para processá-los. Isso desacopla a recepção do processamento:\n\n```python\nclass BufferCircular:\n    def __init__(self, capacidade):\n        self.buffer = [None] * capacidade\n        self.capacidade = capacidade\n        self.head = 0  # posição de leitura\n        self.tail = 0  # posição de escrita\n        self.tamanho = 0\n\n    def enqueue(self, byte):\n        if self.tamanho == self.capacidade:\n            raise OverflowError(\"Buffer cheio! Dado perdido.\")\n        self.buffer[self.tail] = byte\n        self.tail = (self.tail + 1) % self.capacidade\n        self.tamanho += 1\n\n    def dequeue(self):\n        if self.tamanho == 0:\n            return None\n        dado = self.buffer[self.head]\n        self.head = (self.head + 1) % self.capacidade\n        self.tamanho -= 1\n        return dado\n```\n\n## Comparativo: pilha vs. fila\n\n| Característica | Pilha (Stack) | Fila (Queue) |\n|---|---|---|\n| Princípio | LIFO — último a entrar, primeiro a sair | FIFO — primeiro a entrar, primeiro a sair |\n| Inserção | Sempre no topo (push) | Sempre no fim (enqueue) |\n| Remoção | Sempre do topo (pop) | Sempre da frente (dequeue) |\n| Complexidade | O(1) push e pop | O(1) enqueue e dequeue |\n| Uso típico | Recursão, desfazer ações, expressões | Buffers, escalonamento, BFS em grafos |\n| Em Python | `list` com append/pop | `deque` com append/popleft |\n\n## Análise de complexidade\n\nA eficiência das operações é um critério fundamental na escolha de\nestruturas de dados. Pilhas e filas se destacam pela constância de suas\noperações principais:\n\n- **O(1)** para push/pop (pilha): inserção e remoção no topo são sempre\n  constantes, independente do tamanho.\n- **O(1)** para enqueue/dequeue (fila com `deque`): ambas as operações\n  são O(1) garantido em Python.\n- **O(n)** para busca: acessar elementos intermediários exige percorrer\n  toda a estrutura — não é o ponto forte dessas estruturas.\n- **O(n)** de espaço: a memória usada é proporcional ao número de\n  elementos armazenados.\n\nAtenção: usar `list.pop(0)` em Python para simular uma fila é O(n) —\nevite. Use sempre `collections.deque` para filas eficientes.\n\n## Aplicações em engenharia elétrica\n\nPilhas e filas aparecem em praticamente todos os domínios da engenharia\nelétrica moderna:\n\n- **Redes e protocolos** — roteadores utilizam filas de pacotes para\n  gerenciar tráfego; algoritmos como BFS, usados para encontrar o menor\n  caminho em topologias de rede, usam filas diretamente.\n- **Processadores e sistemas operacionais** — a CPU usa uma pilha de\n  hardware para chamadas de função e interrupções; o escalonador de\n  processos usa filas de prioridade para decidir qual tarefa executa a\n  seguir.\n- **Processamento de sinais** — janelas deslizantes para filtros FIR são\n  implementadas com filas circulares: a cada novo sample, enqueue do novo\n  e dequeue do mais antigo, em O(1).\n\n## Exercício final\n\n**Desafio prático:** implemente em Python um simulador de escalonador de\ntarefas para um microcontrolador que:\n\n1. Usa uma **fila de prioridade** para gerenciar 5 tarefas com\n   prioridades diferentes.\n2. Usa uma **pilha** para registrar o histórico de execuções (log).\n3. Permite desfazer (undo) a última tarefa executada.\n\n**Resumo dos conceitos-chave:**\n\n- Pilha: LIFO — push, pop, peek em O(1).\n- Fila: FIFO — enqueue, dequeue em O(1).\n- Buffer circular: fila de tamanho fixo para hardware.\n- Deque: generalização com duas extremidades ativas.\n- Em Python: use `list` para pilhas e `deque` para filas.\n","srcMarkdownNoYaml":"\n\n## Introdução\n\nEsta aula explora dois dos tipos de estruturas de dados lineares mais\nimportantes da computação: **pilhas** e **filas**. O percurso segue uma\nprogressão lógica do conceito à prática: fundamentos, pilhas, filas,\naplicações práticas em engenharia e exercícios de fixação.\n\n## O que são estruturas de dados\n\nUma **estrutura de dados** é uma forma organizada de armazenar, gerenciar\ne acessar informações na memória do computador. A escolha correta da\nestrutura impacta diretamente o desempenho e a eficiência dos algoritmos.\nElas se dividem em três grandes famílias:\n\n- **Lineares** — elementos organizados em sequência: arrays, listas,\n  pilhas e filas.\n- **Hierárquicas** — elementos em relações pai-filho: árvores e heaps.\n- **Em rede** — conexões arbitrárias entre elementos: grafos e tabelas\n  hash.\n\n## Por que estudar pilhas e filas\n\nPilhas e filas estão presentes em sistemas operacionais, compiladores,\nprotocolos de rede, microcontroladores e sistemas de tempo real — todos\nfundamentais para o engenheiro elétrico moderno. Algumas aplicações\ndiretas: gerenciamento de interrupções em microcontroladores (Arduino,\nSTM32), buffers de dados em comunicação serial (UART, SPI, I2C), análise\nde expressões matemáticas em calculadoras, escalonamento de tarefas em\nsistemas embarcados e controle de fluxo em redes de computadores.\n\n## Pilhas (Stacks)\n\n### Conceito e princípio LIFO\n\nUma **pilha (stack)** é uma estrutura de dados linear que segue o\nprincípio **LIFO — Last In, First Out**: o elemento inserido por último é\nsempre o primeiro a ser removido, como uma pilha de pratos — sempre se\nretira e coloca pratos pelo topo.\n\nAs operações fundamentais, todas O(1):\n\n- **PUSH** — insere um elemento no topo da pilha.\n- **POP** — remove o elemento no topo da pilha.\n- **PEEK / TOP** — consulta o topo sem remover o elemento.\n- **isEmpty** — verifica se a pilha está vazia antes de operar, evitando\n  erros de underflow.\n\nO **ponteiro de topo** é o único ponto de acesso da pilha. Tentar remover\nde uma pilha vazia gera um erro chamado **stack underflow**; inserir além\nda capacidade gera **stack overflow**. Uma sequência típica de operações\n— `Push(A)`, `Push(B)`, `Push(C)`, `Pop()` — mostra o ponteiro de topo se\nmovendo a cada chamada.\n\n### Implementação em Python\n\nPython oferece duas formas principais de implementar pilhas: usando\n`list` nativa, ou encapsulando o comportamento em uma classe.\n\nCom lista:\n\n```python\npilha = []\n\n# PUSH - empilhar\npilha.append(10)\npilha.append(20)\npilha.append(30)\nprint(pilha)  # [10, 20, 30]\n\n# PEEK - consultar topo\ntopo = pilha[-1]\nprint(topo)   # 30\n\n# POP - desempilhar\nelemento = pilha.pop()\nprint(elemento)  # 30\nprint(pilha)     # [10, 20]\n\nif not pilha:\n    print(\"Pilha vazia!\")\n```\n\nCom classe orientada a objetos:\n\n```python\nclass Pilha:\n    def __init__(self):\n        self._dados = []\n\n    def push(self, item):\n        self._dados.append(item)\n\n    def pop(self):\n        if self.is_empty():\n            raise IndexError(\"Underflow!\")\n        return self._dados.pop()\n\n    def peek(self):\n        return self._dados[-1]\n\n    def is_empty(self):\n        return len(self._dados) == 0\n\n    def size(self):\n        return len(self._dados)\n\np = Pilha()\np.push(5)\np.push(10)\nprint(p.peek())  # 10\nprint(p.pop())   # 10\n```\n\n### Aplicação prática: verificador de parênteses\n\nUm dos usos clássicos de pilhas é a **verificação de expressões\nbalanceadas**, fundamental em compiladores e parsers de linguagens. O\nalgoritmo percorre a string, empilhando aberturas e verificando se cada\nfechamento corresponde à última abertura pendente:\n\n```python\ndef verificar_parenteses(expressao):\n    \"\"\"Verifica se parênteses, colchetes e chaves estão balanceados.\"\"\"\n    pilha = []\n    pares = {')': '(', ']': '[', '}': '{'}\n    abertos = set('([{')\n\n    for char in expressao:\n        if char in abertos:\n            pilha.append(char)\n        elif char in pares:\n            if not pilha or pilha[-1] != pares[char]:\n                return False\n            pilha.pop()\n\n    return len(pilha) == 0\n\nprint(verificar_parenteses(\"(a + b) * [c - d]\"))  # True\nprint(verificar_parenteses(\"(x + [y))\"))          # False\nprint(verificar_parenteses(\"{V = I * R}\"))        # True\n```\n\nEsse algoritmo roda em O(n), onde n é o tamanho da expressão, e é usado\nem IDEs, compiladores (Python, Java, C++) e em avaliadores de circuitos\nlógicos.\n\n### Pilhas em sistemas embarcados: call stack\n\nEm microcontroladores como **Arduino (ATmega328)** e **STM32**, a pilha\nde chamadas (*call stack*) gerencia automaticamente o retorno de\nsub-rotinas e o contexto de interrupções. Quando ocorre uma interrupção,\no estado dos registradores é empilhado; ao finalizar, é desempilhado.\n\n- **Stack Pointer (SP)** — registrador dedicado que aponta sempre para o\n  topo da pilha de hardware. Em ARM Cortex-M, cresce para endereços\n  menores (pilha descendente).\n- **Context Saving** — ao entrar em uma ISR (Interrupt Service Routine),\n  PC, LR e registradores são automaticamente empilhados pelo hardware\n  para preservar o estado do programa.\n\n## Filas (Queues)\n\n### Conceito e princípio FIFO\n\nUma **fila (queue)** é uma estrutura de dados linear que segue o\nprincípio **FIFO — First In, First Out**: o elemento inserido primeiro é\nsempre o primeiro a ser removido, como uma fila de atendimento bancário.\nA inserção ocorre no **fim (rear)** e a remoção na **frente (front)**.\n\nOperações fundamentais, todas O(1):\n\n- **ENQUEUE** — insere no final da fila (rear/back).\n- **DEQUEUE** — remove da frente da fila (front/head).\n- **FRONT** — consulta o primeiro elemento sem remover.\n\nA diferença fundamental em relação à pilha é que a **fila tem duas\nextremidades ativas**. Isso exige atenção especial na implementação com\narrays, onde o deslocamento de memória pode tornar o dequeue ineficiente\n(O(n)) se não for tratado com ponteiros circulares. Uma sequência típica\n— `Enqueue(A)`, `Enqueue(B,C)`, `Dequeue() → A`, `Dequeue() → B` — ilustra\nos elementos entrando pelo rear e saindo pelo front.\n\n### Implementação em Python\n\nPython oferece `collections.deque` como implementação otimizada para\nfilas, com operações O(1) em ambas as extremidades:\n\n```python\nfrom collections import deque\n\nfila = deque()\n\nfila.append(\"Tarefa_1\")\nfila.append(\"Tarefa_2\")\nfila.append(\"Tarefa_3\")\nprint(fila)\n# deque(['Tarefa_1','Tarefa_2','Tarefa_3'])\n\nprint(fila[0])  # Tarefa_1 (FRONT)\n\natendido = fila.popleft()\nprint(atendido)  # Tarefa_1\nprint(fila)\n# deque(['Tarefa_2', 'Tarefa_3'])\n\nprint(len(fila) == 0)  # False\n```\n\nTambém é possível implementar uma fila com nó encadeado, sem depender de\n`deque`:\n\n```python\nclass No:\n    def __init__(self, dado):\n        self.dado = dado\n        self.proximo = None\n\nclass Fila:\n    def __init__(self):\n        self.front = None\n        self.rear = None\n        self._tamanho = 0\n\n    def enqueue(self, item):\n        novo = No(item)\n        if self.rear:\n            self.rear.proximo = novo\n        self.rear = novo\n        if not self.front:\n            self.front = novo\n        self._tamanho += 1\n\n    def dequeue(self):\n        if not self.front:\n            raise IndexError(\"Fila vazia!\")\n        dado = self.front.dado\n        self.front = self.front.proximo\n        if not self.front:\n            self.rear = None\n        self._tamanho -= 1\n        return dado\n```\n\n### Variações de filas\n\nExistem variações especializadas da fila básica que atendem requisitos\nespecíficos de sistemas reais, especialmente em tempo real e em\ncomunicações:\n\n- **Fila circular** — utiliza um array com ponteiros `front` e `rear`\n  que \"giram\" em módulo, eliminando desperdício de memória. Ideal para\n  buffers de hardware de tamanho fixo.\n- **Fila de prioridade** — cada elemento possui uma prioridade associada,\n  e o dequeue sempre retira o elemento de maior prioridade,\n  independentemente da ordem de inserção. Normalmente implementada com\n  heap.\n- **Deque (double-ended)** — permite inserção e remoção em ambas as\n  extremidades, generalizando pilhas e filas. Em Python,\n  `collections.deque` implementa exatamente esse tipo.\n\n### Buffer circular: aplicação em UART\n\nEm sistemas embarcados, o **buffer circular (ring buffer)** é a\nimplementação de fila mais utilizada para comunicação serial UART. A ISR\nde recepção faz *enqueue* dos bytes recebidos; a rotina principal faz\n*dequeue* para processá-los. Isso desacopla a recepção do processamento:\n\n```python\nclass BufferCircular:\n    def __init__(self, capacidade):\n        self.buffer = [None] * capacidade\n        self.capacidade = capacidade\n        self.head = 0  # posição de leitura\n        self.tail = 0  # posição de escrita\n        self.tamanho = 0\n\n    def enqueue(self, byte):\n        if self.tamanho == self.capacidade:\n            raise OverflowError(\"Buffer cheio! Dado perdido.\")\n        self.buffer[self.tail] = byte\n        self.tail = (self.tail + 1) % self.capacidade\n        self.tamanho += 1\n\n    def dequeue(self):\n        if self.tamanho == 0:\n            return None\n        dado = self.buffer[self.head]\n        self.head = (self.head + 1) % self.capacidade\n        self.tamanho -= 1\n        return dado\n```\n\n## Comparativo: pilha vs. fila\n\n| Característica | Pilha (Stack) | Fila (Queue) |\n|---|---|---|\n| Princípio | LIFO — último a entrar, primeiro a sair | FIFO — primeiro a entrar, primeiro a sair |\n| Inserção | Sempre no topo (push) | Sempre no fim (enqueue) |\n| Remoção | Sempre do topo (pop) | Sempre da frente (dequeue) |\n| Complexidade | O(1) push e pop | O(1) enqueue e dequeue |\n| Uso típico | Recursão, desfazer ações, expressões | Buffers, escalonamento, BFS em grafos |\n| Em Python | `list` com append/pop | `deque` com append/popleft |\n\n## Análise de complexidade\n\nA eficiência das operações é um critério fundamental na escolha de\nestruturas de dados. Pilhas e filas se destacam pela constância de suas\noperações principais:\n\n- **O(1)** para push/pop (pilha): inserção e remoção no topo são sempre\n  constantes, independente do tamanho.\n- **O(1)** para enqueue/dequeue (fila com `deque`): ambas as operações\n  são O(1) garantido em Python.\n- **O(n)** para busca: acessar elementos intermediários exige percorrer\n  toda a estrutura — não é o ponto forte dessas estruturas.\n- **O(n)** de espaço: a memória usada é proporcional ao número de\n  elementos armazenados.\n\nAtenção: usar `list.pop(0)` em Python para simular uma fila é O(n) —\nevite. Use sempre `collections.deque` para filas eficientes.\n\n## Aplicações em engenharia elétrica\n\nPilhas e filas aparecem em praticamente todos os domínios da engenharia\nelétrica moderna:\n\n- **Redes e protocolos** — roteadores utilizam filas de pacotes para\n  gerenciar tráfego; algoritmos como BFS, usados para encontrar o menor\n  caminho em topologias de rede, usam filas diretamente.\n- **Processadores e sistemas operacionais** — a CPU usa uma pilha de\n  hardware para chamadas de função e interrupções; o escalonador de\n  processos usa filas de prioridade para decidir qual tarefa executa a\n  seguir.\n- **Processamento de sinais** — janelas deslizantes para filtros FIR são\n  implementadas com filas circulares: a cada novo sample, enqueue do novo\n  e dequeue do mais antigo, em O(1).\n\n## Exercício final\n\n**Desafio prático:** implemente em Python um simulador de escalonador de\ntarefas para um microcontrolador que:\n\n1. Usa uma **fila de prioridade** para gerenciar 5 tarefas com\n   prioridades diferentes.\n2. Usa uma **pilha** para registrar o histórico de execuções (log).\n3. Permite desfazer (undo) a última tarefa executada.\n\n**Resumo dos conceitos-chave:**\n\n- Pilha: LIFO — push, pop, peek em O(1).\n- Fila: FIFO — enqueue, dequeue em O(1).\n- Buffer circular: fila de tamanho fixo para hardware.\n- Deque: generalização com duas extremidades ativas.\n- Em Python: use `list` para pilhas e `deque` para filas.\n"},"formats":{"html":{"identifier":{"display-name":"HTML","target-format":"html","base-format":"html"},"execute":{"fig-width":7,"fig-height":5,"fig-format":"retina","fig-dpi":96,"df-print":"default","error":false,"eval":true,"cache":null,"freeze":false,"echo":true,"output":true,"warning":true,"include":true,"keep-md":false,"keep-ipynb":false,"ipynb":null,"enabled":null,"daemon":null,"daemon-restart":false,"debug":false,"ipynb-filters":[],"ipynb-shell-interactivity":null,"plotly-connected":true,"engine":"markdown"},"render":{"keep-tex":false,"keep-typ":false,"keep-source":false,"keep-hidden":false,"prefer-html":false,"output-divs":true,"output-ext":"html","fig-align":"default","fig-pos":null,"fig-env":null,"code-fold":"none","code-overflow":"scroll","code-link":false,"code-line-numbers":false,"code-tools":false,"tbl-colwidths":"auto","merge-includes":true,"inline-includes":false,"preserve-yaml":false,"latex-auto-mk":true,"latex-auto-install":true,"latex-clean":true,"latex-min-runs":1,"latex-max-runs":10,"latex-makeindex":"makeindex","latex-makeindex-opts":[],"latex-tlmgr-opts":[],"latex-input-paths":[],"latex-output-dir":null,"link-external-icon":false,"link-external-newwindow":false,"self-contained-math":false,"format-resources":[],"notebook-links":true},"pandoc":{"standalone":true,"wrap":"none","default-image-extension":"png","to":"html","css":["../../styles.css"],"toc":false,"output-file":"notas.html"},"language":{"toc-title-document":"Table of contents","toc-title-website":"On this page","related-formats-title":"Other Formats","related-notebooks-title":"Notebooks","source-notebooks-prefix":"Source","other-links-title":"Other Links","code-links-title":"Code Links","launch-dev-container-title":"Launch Dev Container","launch-binder-title":"Launch Binder","article-notebook-label":"Article Notebook","notebook-preview-download":"Download Notebook","notebook-preview-download-src":"Download Source","notebook-preview-back":"Back to Article","manuscript-meca-bundle":"MECA Bundle","section-title-abstract":"Abstract","section-title-appendices":"Appendices","section-title-footnotes":"Footnotes","section-title-references":"References","section-title-reuse":"Reuse","section-title-copyright":"Copyright","section-title-citation":"Citation","appendix-attribution-cite-as":"For attribution, please cite this work as:","appendix-attribution-bibtex":"BibTeX citation:","appendix-view-license":"View License","title-block-author-single":"Author","title-block-author-plural":"Authors","title-block-affiliation-single":"Affiliation","title-block-affiliation-plural":"Affiliations","title-block-published":"Published","title-block-modified":"Modified","title-block-keywords":"Keywords","callout-tip-title":"Tip","callout-note-title":"Note","callout-warning-title":"Warning","callout-important-title":"Important","callout-caution-title":"Caution","code-summary":"Code","code-tools-menu-caption":"Code","code-tools-show-all-code":"Show All Code","code-tools-hide-all-code":"Hide All Code","code-tools-view-source":"View Source","code-tools-source-code":"Source Code","tools-share":"Share","tools-download":"Download","code-line":"Line","code-lines":"Lines","copy-button-tooltip":"Copy to Clipboard","copy-button-tooltip-success":"Copied!","repo-action-links-edit":"Edit this page","repo-action-links-source":"View source","repo-action-links-issue":"Report an issue","back-to-top":"Back to top","search-no-results-text":"No results","search-matching-documents-text":"matching documents","search-copy-link-title":"Copy link to search","search-hide-matches-text":"Hide additional matches","search-more-match-text":"more match in this document","search-more-matches-text":"more matches in this document","search-clear-button-title":"Clear","search-text-placeholder":"","search-detached-cancel-button-title":"Cancel","search-submit-button-title":"Submit","search-label":"Search","toggle-section":"Toggle section","toggle-sidebar":"Toggle sidebar navigation","toggle-dark-mode":"Toggle dark mode","toggle-reader-mode":"Toggle reader mode","toggle-navigation":"Toggle navigation","crossref-fig-title":"Figure","crossref-tbl-title":"Table","crossref-lst-title":"Listing","crossref-thm-title":"Theorem","crossref-lem-title":"Lemma","crossref-cor-title":"Corollary","crossref-prp-title":"Proposition","crossref-cnj-title":"Conjecture","crossref-def-title":"Definition","crossref-exm-title":"Example","crossref-exr-title":"Exercise","crossref-ch-prefix":"Chapter","crossref-apx-prefix":"Appendix","crossref-sec-prefix":"Section","crossref-eq-prefix":"Equation","crossref-lof-title":"List of Figures","crossref-lot-title":"List of Tables","crossref-lol-title":"List of Listings","environment-proof-title":"Proof","environment-remark-title":"Remark","environment-solution-title":"Solution","listing-page-order-by":"Order By","listing-page-order-by-default":"Default","listing-page-order-by-date-asc":"Oldest","listing-page-order-by-date-desc":"Newest","listing-page-order-by-number-desc":"High to Low","listing-page-order-by-number-asc":"Low to High","listing-page-field-date":"Date","listing-page-field-title":"Title","listing-page-field-description":"Description","listing-page-field-author":"Author","listing-page-field-filename":"File Name","listing-page-field-filemodified":"Modified","listing-page-field-subtitle":"Subtitle","listing-page-field-readingtime":"Reading Time","listing-page-field-wordcount":"Word Count","listing-page-field-categories":"Categories","listing-page-minutes-compact":"{0} min","listing-page-category-all":"All","listing-page-no-matches":"No matching items","listing-page-words":"{0} words","listing-page-filter":"Filter","draft":"Draft"},"metadata":{"lang":"en","fig-responsive":true,"quarto-version":"1.10.18","engines":[{"path":"/Applications/quarto/share/extension-subtrees/julia-engine/_extensions/julia-engine/julia-engine.js"}],"theme":{"light":"flatly","dark":"darkly"},"page-layout":"full","title":"Notas — Aula 01: Estruturas de Dados — Pilhas e Filas"},"extensions":{"book":{"multiFile":true}}}},"projectFormats":["html"]}