---
title: "Gabarito — Aula 00: Revisão de Lógica de Programação"
---

Soluções da [atividade desta aula](atividade.qmd). Todo o código abaixo
foi executado e a saída mostrada é exatamente o que o Python produz —
não é uma estimativa.

## Atividade 1 — Gabarito

```python
a = int(input("Digite o valor de a: "))
b = int(input("Digite o valor de b: "))
soma = a + b
print(f"A soma de a e b é: {soma}")
```

Saída verificada (entrada `5`, `3`):

```
Digite o valor de a: 5
Digite o valor de b: 3
A soma de a e b é: 8
```

**Gabarito da variação** (menu soma/subtração):

```python
a = int(input("Digite o valor de a: "))
b = int(input("Digite o valor de b: "))
print("1 - Soma")
print("2 - Subtração")
op = int(input("Escolha uma operação: "))
if op == 1:
    print(f"Resultado da soma: {a + b}")
elif op == 2:
    print(f"Resultado da subtração: {a - b}")
```

Saída verificada (entrada `10`, `4`, `2`):

```
Digite o valor de a: 10
Digite o valor de b: 4
1 - Soma
2 - Subtração
Escolha uma operação: 2
Resultado da subtração: 6
```

## Atividade 2 — Gabarito

```python
n = int(input("Quantos números você deseja somar? "))
soma = 0
for i in range(1, n + 1):
    numero = int(input(f"Digite o {i}º número: "))
    soma += numero
print(f"A soma de todos os números é: {soma}")
```

Saída verificada (entrada `3`, `10`, `5`, `2`):

```
Quantos números você deseja somar? 3
Digite o 1º número: 10
Digite o 2º número: 5
Digite o 3º número: 2
A soma de todos os números é: 17
```

## Atividade 3 — Gabarito

```python
n = int(input("Quantos números deseja informar? "))
pares = 0
impares = 0
for i in range(1, n + 1):
    numero = int(input(f"Digite o {i}º número: "))
    if numero % 2 == 0:
        print(f"{numero} é par.")
        pares += 1
    else:
        print(f"{numero} é ímpar.")
        impares += 1
print(f"Quantidade de números pares: {pares}")
print(f"Quantidade de números ímpares: {impares}")
```

Saída verificada (entrada `4`, `10`, `7`, `4`, `9`):

```
Quantos números deseja informar? 4
Digite o 1º número: 10
10 é par.
Digite o 2º número: 7
7 é ímpar.
Digite o 3º número: 4
4 é par.
Digite o 4º número: 9
9 é ímpar.
Quantidade de números pares: 2
Quantidade de números ímpares: 2
```

## Atividade 4 — Gabarito

```python
while True:
    print("1 - Somar")
    print("2 - Subtrair")
    print("3 - Multiplicar")
    print("0 - Sair")
    opcao = int(input("Escolha uma opção: "))

    if opcao == 0:
        print("Programa encerrado.")
        break
    elif opcao in (1, 2, 3):
        a = int(input("Digite o primeiro número: "))
        b = int(input("Digite o segundo número: "))
        if opcao == 1:
            resultado = a + b
        elif opcao == 2:
            resultado = a - b
        else:
            resultado = a * b
        print(f"Resultado: {resultado}")
    else:
        print("Opção inválida")
```

Saída verificada, para a entrada `1`, `10`, `5`, `3`, `4`, `5`, `0`
(soma, depois multiplicação, depois sai):

```
1 - Somar
2 - Subtrair
3 - Multiplicar
0 - Sair
Escolha uma opção: 1
Digite o primeiro número: 10
Digite o segundo número: 5
Resultado: 15
1 - Somar
2 - Subtrair
3 - Multiplicar
0 - Sair
Escolha uma opção: 3
Digite o primeiro número: 4
Digite o segundo número: 5
Resultado: 20
1 - Somar
2 - Subtrair
3 - Multiplicar
0 - Sair
Escolha uma opção: 0
Programa encerrado.
```

Também testamos o caminho de opção inválida: com a entrada `9`, `1`,
`2`, `3`, `0`, o programa imprime "Opção inválida" para a opção `9`,
depois processa normalmente a soma `1 + 2 = 3` na opção `1`, e encerra na
opção `0`.
